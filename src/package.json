{
    "name": "DB",
    "version": "1.0.0",
"scripts": {
  "json-server": "json-server --watch db.json --port 3000",
  "start": "npm run json-server"
}
,
   
    "devDependencies": {
      "json-server": "^0.16.3",
      "concurrently": "^6.0.0"
    }
  }
  