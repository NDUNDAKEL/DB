{
    "name": "DB",
    "version": "1.0.0",
"scripts": {
  "start": "json-server --watch db.json --port 10000 --host 0.0.0.0"
}
,
   
    "devDependencies": {
      "json-server": "^0.16.3",
      "concurrently": "^6.0.0"
    }
  }
  